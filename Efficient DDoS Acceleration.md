專題二：基於 CPU 架構之高效能 DDoS 偵測加
速系統
摘要與研究成果
研究針對高速網路 DDoS 攻擊偵測，提出一
套基於 CPU AVX-512 向量化 的滑動窗口熵加速
方法。傳統熵計算對百萬級資料存在高成本，尤其
是 log!(𝑐#) 運算耗時顯著 。為此， 研究採用三階泰
勒多項式結合 Horner 法近似 log!(1 + 𝑥)，並將位
元 組 頻 率 直 接 載 入 AVX-512 寄 存 器 ， 透 過
\_mm512_fmadd_ps 與遮罩運算實現並行累加，將
原本 𝑂(𝑛 ⋅ 𝑤) 的計算壓縮至近似 𝑂(𝑛)，顯著提升
性能。實驗結果在單精度運算下，三階泰勒多項式
近似比 C++ Baseline 與 NumPy 分別加速 5.82
倍與 9.42 倍，誤差 MAE 僅 1. 3 × 10VW。對不同
窗口大小（100、512、1,024、4,096）測試，AVX-
512 平均執行時間保持在 0.046–0.059 秒，最大加
速比達 406 倍， 且熵計算精度仍在可接受範圍， 適
合即時偵測。即使在無 GPU 的企業或邊緣設備上，
也能透過 AVX-512 向量化與多層次 CPU 優化實
現高效、 準確的 DDoS 偵測，提供可行的即時防禦
方案。

1. 研究動機
   在高速網路環境中，DDoS 攻擊偵測不僅需要
   高準確度，更需具備極低延遲與高吞吐量。基於過
   去的研究 An Efficient DDoS Attack Detection System
   Based on Multi-Entropy Clustering and the Mamba
   Model， 雖提出了多熵分群與 Mamba 模型的架構，
   取得了不錯的辨識效果，但在具體的熵計算與性能
   優化策略方面討論甚少。即使是數毫秒的運算瓶頸，
   也可能導致系統延遲或誤判。
   基於此緣故，專題希望能補足這一缺口，探索
   結合 AVX-512 向量化指令集 的 CPU 加速策略，
   對滑動窗口熵計算進行底層優化。考量到許多企業
   內部或邊緣設備無法配置獨立 GPU，CPU 依然是
   主要運算平台，因此希望透過高效的平行計算與指
   令集優化，實現即使在 CPU 上也能高效完成
   DDoS 偵測的目標。
2. 研究方法
   13
   2.1 理論基礎與問題定義
   透 過 我 An Efficient DDoS Attack Detection
   System Based on Multi-Entropy Clustering and the
   Mamba Mode 的研究結論可知熵非常適合在 DDoS
   檢測之中。 資訊熵 (Information Entropy) ： 對於一個
   離散隨機變數 X，其資訊熵 𝐻(𝑋) 定義為：
   K
   𝐻(𝑋) = − Ü 𝑝# #$%
log! 𝑝#
𝑝# 是變數 𝑋 取第 𝑖 個值的機率。在的應用中，處
理的是位元組流，因此 𝑖 的範圍是 [0, 255]。 （為了
減少計算量） 。滑動窗口熵實作公式對於一個長度
為 𝑊 的滑動窗口，若某個位元組值 𝑖 出現了 𝑐#
次，則其熵值可以透過以下數值穩定的形式計算，
此形式也更適合向量化處理：
𝐻 = log! 𝑊 −
!XX
1
𝑊 Ü 𝑐#
#$Y
   log! 𝑐# (for 𝑐# > 0)
   此公式形式不僅數值穩定，也更適合於向量化處理。
   然而在計算 log!(𝑐#) 仍屬高成本操作， 對百萬級滑
   動窗口尤其耗時。為了加速採用多項式近似的策略，
   對 log!(1 + 𝑥)進行泰勒展開， 截斷至三階多項式，
   為 了 在 進 一 步 降 低 計 算 量 ， 採 用 Horner 法
   （Horner’s Method） 重寫多項式。Horner 法是一種
   多項式計算技巧，核心思想是將多項式的冪次計算
   轉化為 連鎖乘加 的形式，避免重複計算高次方。
   三階多項式原式：
   𝑃(𝑥) = 𝑐Y + 𝑐%𝑥 + 𝑐!𝑥! + 𝑐W𝑥W
   可以用 Horner 法重寫為：
   計算 𝑃(𝑥) 僅需三次乘加，無需額外計算 𝑥!與 𝑥W ，
   大幅降低運算量。
   2.2 熵計算加速流程圖
   圖 1：計算流程圖
   將 滑 動 窗 口 的 位 元 組 頻 率 𝑐# 向 量 化 載 入
   AVX-512 暫存器，轉浮點後用三階多項式近似
   log2，透過遮罩避免無效計算，再進行並行累加得
   到熵值 𝐻。
3. 資料向量化設計 ： 我將滑動窗口中的位元組頻
   率 𝑐# 直接載入 AVX-512 暫存器，實現 「一次
   處 理 16 個 元 素 」 的 並 行 化 。 使 用
   𝑚𝑚512
   \_
   *𝑐𝑣𝑡𝑒𝑝𝑖32
   *𝑝𝑠 將整數計數轉換為浮點
   數，以便進行對數運算。這部分讓原本逐元素
   的 𝑂(𝑛 · 𝑤) 計算， 壓縮成近似 𝑂(𝑛) 的處理流
   程，其中 𝑛 為資料長度，𝑤 為窗口大小。
4. 對數近似與遮罩運算 ： 設計了一個多項式近似
   log2 函數，並透過 *
   𝑚𝑚512
   *𝑓𝑚𝑎𝑑𝑑\*𝑝𝑠 （乘加
   融合指令）把多項式展開壓縮到單條指令完成，
   大 幅 減 少 指 令 數 。 在 透 過
   𝑚𝑚512
   𝑚𝑎𝑠𝑘𝑧
   𝑚𝑢𝑙
   - \_
     \_
     \_𝑝𝑠 遮罩避免無效計算。
5. 並 行 累 加 優 化 ： 使 用 _
   𝑚𝑚512
   𝑎𝑑𝑑
   _
   _𝑝𝑠 與
   𝑚𝑚512
   𝑎𝑑𝑑
   _
   \_
   \_𝑒𝑝𝑖32 完成 𝑐# log!(𝑐#) 的並行
   累加，避免序列式加總的瓶頸。
6. 實驗結果：
   為評估多項式近似的精度，本研究使用 Mean
   Absolute Error (MAE) 量化預測值與真實值之間
   的平均絕對差異，其定義為：
   K
   1
   𝑛 Ü|𝑦#− 𝑦Z
   ó|
   #$%
   其中 𝑦# 為真實值， 𝑦Z
   ó 為預測值， 𝑛 為樣本數。 MAE
   越小表示模型或近似方法的預測越接近真實值。
   3.1 對數近似效能
   MAE =
   14
   在 𝑥 ∈ [0,1] 區間內，採用三階泰勒多項式
   進行近似，並與 C++ Baseline 與 NumPy 函數進
   行比較。實驗分為雙精度（Double Precision）與單
   精度（Single Precision）兩種設定， 評估其準確度與
   運算效能。
   實驗結果顯示 ，在單精度設定下，多項式近似
   的 計 算 速 度 分 別 是 C++ Baseline 的 5.82 倍 和
   NumPy 的 9.42 倍，而誤差（MAE）控制在可接受
   的範圍內。這證明犧牲微小精度來換取巨大效能提
   升的策略是完全可行的。
   項目 Double Single
   多項式近似 MAE 1.3
   1.3 × 10VW
   × 10VW
   多 項 式 近 似 時 間
   (µs)
   681.69 µs 52.19 µs
   Baseline 時間(µs) 729.23 303.93
   NumPy 時間(µs) 1154.71 491.56
   NumPy 加速比 1.69 倍 9.42 倍
   Baseline 加速比 1.07 倍 5.82 倍
   表 1：速度比較圖比較表
   3.2 系統整體效能：
   對不同滑動窗口大小（100、 512、 1024、 4096）
   測試 Cython Baseline 與 C++ AVX-512 的效能。結
   果顯示， AVX-512 在所有窗口下皆顯著加速，平均
   執行時間維持在 0.046~0.059 秒，而 Baseline 隨
   窗口增大線性上升，最大加速比達 406 倍。 使用三
   階泰勒多項式近似計算 log!(1 + 𝑥)的誤差隨窗口
   略增，但仍在可接受範圍，證明該方法能在保持足
   夠精度的同時大幅提升計算效率。
   Window
   Size
   Method Avg
   Time
   (s)
   Speedup
   vs
   Cython
   100 Cython Baseline 1.6380 1.00×
   100 C++ AVX-512 0.0487 33.64×
   512 Cython Baseline 4.9548 1.00×
   512 C++ AVX-512 0.0586 84.50×
   1,024 Cython Baseline 6.1399 1.00×
   1,025 C++ AVX-512 0.0461 133.07×
   4,096 Cython Baseline 18.8877 1.00×
   4,096 C++ AVX-512 0.0465 406.02×
   表 2：不同窗口大小下的效能比較表
7. 結論：
   提出基於 AVX-512 的向量化滑動窗口熵計
   算方法。單精度測試中，多項式近似 log2 運算較
   C++ Baseline 與 NumPy 分 別 加 速 5.82 倍 與
   9.42 倍，最大總體加速比達 406 倍。最終實現高
   效計算熵特徵，並同時兼顧算精度與性能。
