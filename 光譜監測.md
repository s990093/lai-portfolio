專題六（實作）：光譜監測蝕刻電漿大數據之氟化物分析
摘要與研究成果：
研究針對半導體蝕刻製程中龐大的光學發射光譜（OES）數據，設計以 Transformer 為核心的時序分類模型，能自動辨識四大製程階段並進行異常偵測。訓練流程包含訊號平滑與 Min-Max 標準化，並以切片序列搭配交叉熵損失、Adam 優化器進行小批次訓練，透過驗證集早停確保泛化效果。實驗結果顯示，模型達到 96.44% 準確率、AUC 超過 0.99，展現高度穩定與可分性。最終並開發跨平台應用程式，提供即時製程監控與分析。並累積了從資料清理、模型設計、訓練策略到系統整合的完整實務經驗，為未來處理更複雜或缺乏標註的製程數據奠定基礎。

1. 研究目的：
   OES（Optical Emission Spectroscopy, 光學發射光譜是蝕刻製程中常見的非侵入式監控手段，能透過電漿與反應氣體作用所產生的特定波長光訊號進行分析。然而，OES 數據龐大且複雜，人工難以即時判斷異常或穩定區段。以 F 與 Ar 的光譜強度為例，圖 1 約 80–180 秒可視為穩定階段，但在成千上萬筆波形資料中，快速辨識顯得極為困難。因此，研究旨在利用深度學習方法自動化處理 OES 時序數據，以提升製程監控的效率與精度。
   ![alt text](image-10.png)
   ![alt text](image-11.png)
   圖 1：RPS_1000W+In-Line PECVD100W_60S 反應 2. 研究方法：
   本專題旨在利用深度學習技術，自動化分析半導體蝕刻製程中的光學發射光譜（OES）大數據，以達成製程階段辨識與異常偵測。整個研究流程可分為數據預處理、模型設計與訓練，以及實驗驗證與應用實作三大主要階段。
   2.1 數據預處理
   在進入模型訓練前，龐大且複雜的 OES 時序數據需要經過嚴謹的預處理。首先，原始的 \mathbit{F}、Ar、SiF 光譜強度訊號會經過訊號平滑處理，以濾除數據中的高頻雜訊，提升訊號的穩定性。接著，為了消除不同訊號強度範圍的影響，並使數據符合模型輸入要求，將所有訊號進行 Min-Max 標準化，轉換至[0,1]的範圍內，其公式為 X^\prime=\frac{X-\min{\left(X\right)}}{\max{\left(X\right)}-\min{\left(X\right)}}。完成這些步驟後，數據會被切割成固定長度的序列片段，並根據其對應的時間區間標註上製程階段類別(PRE-CLEAN、RAMPING、DEPOSITION、POST-DEPO）。
   2.2 模型設計與訓練
   ![alt text](image-12.png)
   圖 2：訓練流程圖
   研究的核心在於建構一個以 Transformer 為基礎的時序分類模型。原始的 OES 訊號序列（包含 F、Ar、SiF）首先經過位置編碼，以保留訊號的時間順序資訊。隨後，訊號進入由雙層、四頭注意力機制構成的 Transformer Encoder，用於提取數據中的時間序列特徵。為了有效濃縮整段序列的資訊，採用了 Self-Attention Pooling 的方式，生成一個代表性的向量，該向量隨後被輸入至分類器，輸出對應的製程階段機率。
   ![alt text](image-13.png)
   ![alt text](image-14.png)
   圖 3：模型結構圖
   在訓練策略上，模型採用小批次隨機梯度下降（mini-batch SGD）的方式進行訓練，以加速收斂並提升效率。損失函數選用交叉熵（Cross Entropy Loss），此為多分類問題的標準選擇，用於衡量模型預測的製程階段與真實標籤之間的差異。模型的權重更新則透過 Adam 優化器來達成，該優化器以其高效性和穩定性在深度學習領域廣泛應用。為確保模型的泛化能力，在每個訓練週期（epoch）結束後，會使用驗證集來評估模型的表現。若驗證集的表現不再提升，則會啟動早停機制，保存模型在驗證集上表現最佳的權重，避免模型過度擬合訓練數據。 3. 實驗解果
   在實驗結果方面，模型的分類準確率達到 96.44%，AUC 超過 0.99，混淆矩陣顯示分類效果相當理想，並且在 ROC 曲線中展現出高度可分性。這代表模型能夠穩定地自動化辨識製程四大階段，快速偵測潛在異常或停滯段落，協助工程師及早介入。同時，我也開發了批次資料處理與視覺化工具，提升大數據分析效率，避免人工在龐大波形中逐一搜尋的低效作業。
   ![alt text](image-15.png)
   圖 4： 損失與準確率圖

![alt text](image-16.png)
![alt text](image-17.png)
圖 5：混淆矩陣與 ROC 圖
在應用實作上，為了方便工程現場使用，我打造了一個跨平台的應用程式。後端以 Flask 結合 PyTorch 完成推論任我，前端則採用 Electron，並透過 Node.js 打包成獨立 APP，讓工程師能直接使用圖形介面完成即時分析。
![alt text](image-18.png)
圖 5：APP 介面圖 4. 結論
透過這次專題，我學會了從資料清理、模型設計、訓練到系統整合的完整流程，也體會到跨領域合作的重要性。更進一步地，我發現若能先以無監督方式訓練編碼器，將更有助於處理真實製程中多階段且複雜的數據，特別是在缺乏完整標註資料的情況下。這些經驗不僅累積了我的實務能力，也讓我在面對研究型課題時更加得心應手，為我之後進入研究所奠定了良好的訓練
